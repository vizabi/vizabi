language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-yotests' ]; then cd .. && eval "mv $currentfolder generator-yotests" && cd generator-yotests; fi

